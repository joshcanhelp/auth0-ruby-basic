<%= form_with(model: @user, local: true) do |form| %>

  <%= render 'shared/error_output', entity: @user %>

  <div class="form-group">
    <%= form.label :name, class: 'control-label' %>
    <%= form.text_field :name, id: :user_name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :email, class: 'control-label' %>
    <%= form.email_field :email, id: :user_email, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :password, class: 'control-label' %>
    <%= form.password_field :password, id: :user_password, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :password_confirmation, class: 'control-label' %>
    <%= form.password_field :password_confirmation, id: :user_password_confirmation, class: 'form-control' %>
  </div>

  <% if current_user_is_admin? %>
    <div class="form-group">
      <%= form.label :auth0_id, 'Auth0 ID', class: 'control-label' %>
      <%= form.text_field :auth0_id, id: :user_auth0_id, class: 'form-control' %>
    </div>
  <% end %>

  <div class="form-group">
    <%= link_to '← Cancel', @user, class: 'btn btn-default' %>
    <%= form.submit submit_button_text, :class => 'btn btn-primary' %>
  </div>
<% end %>
